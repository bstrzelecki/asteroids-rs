[package]
name = "asteroids-rs"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dependencies]
bevy = { version="0.15.1", features=["wayland", "dynamic_linking"] }
leafwing-input-manager = { version = "0.16.0" }
